name: "Test action"
description: "Test input parameters"
inputs:
  test: # id of input
    description: "test"
    required: true
  another:
    description: "another"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"
